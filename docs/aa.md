希望你
1. 给我讲清楚你怎么实现上下文context的压缩的。我要你实现的是，加入VLM作为用户可以选的一个增强选项，然后take a picture，用上下文的text和那个picture来共同生成表征；另外的话defalut选择可以有一个快速版，就是把chrome里面的source变成text喂给一个强大的text encoder然后压缩成表征，然后在数据库里面做索引。
2. upload这个按钮生成的推荐文件的时候，在点击推荐文件的时候能够直接帮用户在“发现文件”中直接定位到文件位置，然后用户只要点击就行了，然后这个extension还要记住这个path，记在persistent memory里面。

现在的问题是，能否给我生成一个我能看到生成流程的一个workflow打进log里面，比如我进行一次scan之后，我想要看到什么服务被具体调用了，以及这些ranking是怎么生成的，这个ranking依旧不变，我认为有一些问题，以及，我点击的时候依旧会弹出文件导航，而且会有error，而不是直接指定文件直接填进去。